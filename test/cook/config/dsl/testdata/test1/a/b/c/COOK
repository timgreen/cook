// @import "//rules/string"

stringLength("target_abc", "abc")

stringLength(
  name = "target_defg",
  s = "defg"
)

val a = glob("*a*")
val b = glob("*b*")
val c = glob("abc")

val a2 = glob(includes = "*a*", excludes = "aaa")
val b2 = glob(includes = "*b*", excludes = "abc")
