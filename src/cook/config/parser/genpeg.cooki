# Genrule for genpeg
#
# Generate java file from peg define

def genpeg_java(src, parser = "Parser", semantics = "Semantics") {
  # generated_java = path + name + ".java"
  # rule(cmd = "java -cp $CP mouse.Generate -G $src -p cook.parser -P ConfigParser -S ConfigSemantics -D $GEN_DIR/cook/parser/")
}
