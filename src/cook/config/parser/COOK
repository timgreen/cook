include("genpeg")

# Config Parser
genpeg_java(
  name = "gen_parser",
  src = "cook.peg",
  package = "cook.config.parser",
  runtime = "cook.config.parser.runtime",
)

scala_lib(
  name = "semantics",
  srcs = [
    "Semantics.scala",
    "unit/Unit.scala",
    ":gen_parser",
  ],
  deps = [
    "//src/cook/util:util",
    "runtime:runtime",
  ],
)

java_lib(
  name = "parser",
  srcs = [ ":gen_parser" ],
  deps = [
    ":semantics",
    "runtime:runtime",
    # cp
    "//lib/scala-library.jar",
  ],
)

scala_lib(
  name = "cookParser",
  srcs = [
    "CookParser.scala",
  ],
  deps = [
    ":parser",
    "//src/cook/util:util",
  ],
)
