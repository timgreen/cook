scala_lib(
  name = "configtype",
  srcs = [ "ConfigType.scala" ],
)

scala_lib(
  name = "evaluators",
  srcs = [
    "Evaluators.scala",
  ],
  deps = [
    ":configtype",
    "//src/cook/config/parser:parser",
    "//src/cook/config/runner/value:value",
    # cp
    "//lib/ant.jar",
  ],
)

scala_lib(
  name = "runner",
  srcs = [
    "CookRunner.scala",
  ],
  deps = [
    ":evaluators",
    # cp
    "//lib/ant.jar",
  ]
)
